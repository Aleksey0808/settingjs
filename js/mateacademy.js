function makeStickers(detailsCount, robotPart) {
  let value = [];
  if (detailsCount === 0) {
    return [];
}
    for (let i = 0; i < robotPart.length; i++) {
      
  }
}

makeStickers(1, robotPart);